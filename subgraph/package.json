{
  "name": "IP-NFT-GOERLI",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "prepare:local": "mustache config/local.json subgraph.template.yaml > subgraph.yaml",
    "prepare:goerli": "mustache config/goerli.json subgraph.template.yaml > subgraph.yaml",
    "prepare:mainnet": "mustache config/mainnet.json subgraph.template.yaml > subgraph.yaml",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dorianwilhelm/IP-NFT-GOERLI",
    "create-local": "graph create --node http://localhost:8020/ dorianwilhelm/IP-NFT-GOERLI",
    "remove-local": "graph remove --node http://localhost:8020/ dorianwilhelm/IP-NFT-GOERLI",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dorianwilhelm/IP-NFT-GOERLI",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
